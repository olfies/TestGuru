<tr>

  <td><%= test.title%></td>
  <td><%= test.level %></td>
  <td><%= test.questions.count%></td>
  <td><%= link_to ('.show'), tests_path(test) %></td>
  <td><%= link_to t('.edit'), edit_admin_test_path(test) %></td>
  <td>
   <%= link_to t('.delete'), tests_path(test), method: :delete, data: { confirm: 'Are you sure?' } %>
</tr>
    <td><%= button_to t('.start'), start_test_path(test) %></td>
  </td>
