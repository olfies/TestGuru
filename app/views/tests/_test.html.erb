<tr>

  <td><%= test.title%></td>
  <td><%= test.level %></td>
  <td><%= test.questions.count%></td>
  <td><%= link_to 'Show', tests_path(test) %></td>
  <td><%= link_to 'Edit', edit_admin_test_path(test) %></td>
  <td>
   <%= link_to 'Delete', tests_path(test), method: :delete, data: { confirm: 'Are you sure?' } %>
</tr>
    <td><%= button_to 'Start', start_test_path(test) %></td>
  </td>
